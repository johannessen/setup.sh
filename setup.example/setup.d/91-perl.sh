#!bash


# The 'Hello World' site doesn't need Perl, so we don't need to brew.
# This empty script overrides the one provided by default in the
# "global" setup with the same name.

echo "Skipping perlbrew."

